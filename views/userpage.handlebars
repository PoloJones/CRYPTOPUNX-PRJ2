{{#if isLoggedIn}}
<h2>Welcome, {{user.username}}!</h2>
<p>Your id is <strong>{{user.id}}</strong>.</p>
{{/if}}


<!-- 3 crypto cards displaying price at the top (copy from index.html) -->
{{!-- will be replaced with coin API information --}}

{{!-- {{#each coins}}
<div class="flex-container">
  <div class="row" id="ETHER">
    <div class="column">
      <div class="card" id="ethereumCard">
        <div class="card-image">
          <img
            src="https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fs3.tradingview.com%2Fu%2FUCikR7Uz_mid.png&f=1&nofb=1">
          <span class="card-title" id="ethereumName">{{name}}</span>
        </div>
      </div>
    </div>
  </div>
  </div>
  {{/BTC}}

  {{#ETH}}
  <div class="flex-container">
    <div class="row" id="ETHER">
      <div class="column">
        <div class="card" id="ethereumCard">
          <div class="card-image">
            <img
              src="https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fs3.tradingview.com%2Fu%2FUCikR7Uz_mid.png&f=1&nofb=1">
            <span class="card-title" id="ethereumName">{{name}}<p>{{quote.USD.price}}</p></span>
          </div>
          <div class="card-action">
            <a href="#">ABOUT {{name}}</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
{{/each}}  --}}


<!-- Loop over each object in the projects array -->

<div class="container4  postBox" id="existingPosts">

  {{#each posts as |post| }}
  <div class="row center box  ">
    <div class="col s12 indBox">
      <div class="card-content black-text">
        <p>
          {{post.description}}
        </p>
        <p class="boldpost">Created by {{post.name}} on {{format_date date_created}} </p>
      </div>
    </div>
  </div>
  {{/each}}
</div>


<div class="container" id="inputBox">

  <!-- user input for their forum post + submit button -->
  <div class="row center box">
    <form class="col s12 new-post-form">
      <div class="row">
        <p class="bold">Add a post</p>
        <div class="input-field col s12 inputbg">
          <textarea id="username" class="materialize-textarea"></textarea>
          <label for="username">username:</label>
          {{!-- <input class="form-input" type="text" id="username" name="username" /> --}}
          <textarea id="description" class="materialize-textarea"></textarea>
          <div class="row"><label for="description"></label>
            {{!-- <input class="form-input" type="number" id="description" name="description" /> --}}
          </div>

        </div>
      </div>
  </div>
  </form>
  {{!-- <button class="btn waves-effect waves-light" type="submit" name="action">Submit</button> --}}
</div>
<button class="btn waves-effect waves-light" type="submit" name="action">Submit</button>




</div>
<script src="/js/userpage.js"></script>